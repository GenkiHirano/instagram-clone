<% provide(:title, "Search") %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>画像検索</h1>
    <h3>※ 検索のみ実装してます。画像が引っ張って来れません。</h3>
    <section class="micropost_form">
      <%= form_tag(search_path,:method => 'get') do %>
        <%= text_field_tag :search %><br>
        <%= submit_tag 'Search', :content => nil %>
      <% end %>
      <% if @search.present? %>
        <% @search.each do |post| %>
          <%= post.content %><br>
          <%= post.image %><br>
          <%= post.id %><br>
          <%= post.created_at %><br>
          <%= post.updated_at %><br>
        <% end %>
      <% end %>
    </section>
  </div>
</div>
